<!--==========================================
                   ABOUT
===========================================-->
<div id="about" class="section">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <!-- DETAILS -->
                <div id="about-card" class="card">
                    <div class="card-content">
                        <!-- ABOUT PARAGRAPH -->
                        {{ content }}
                    </div>

                    <!-- BUTTONS -->
                    <div id="about-btn" class="card-action">
                        <div class="about-btn">
                            <!-- DOWNLOAD CV BUTTON -->
{#                             {% for button in page.header.buttons %}
                                <a href="{{ button.url }}" class="btn waves-effect">{{ button.text }}</a>
                            {% endfor %} #}
                            <!-- DOWNLOAD CV BUTTON -->
                            <a href="#" class="btn waves-effect">下载传播效果报告</a>
                            <a href="#" data-toggle="modal" data-target="#advModal" class="btn waves-effect">我有好故事</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>